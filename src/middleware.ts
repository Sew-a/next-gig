// Middleware disabled — Next.js 16 deprecated the "middleware" convention in favor of "proxy".
// Re-enable when migrating to the proxy pattern.
// export { default } from "next-auth/middleware"
// 
// export const config = {
//   matcher: ["/dashboard/:path*", "/api/upload", "/api/files/:path*"]
// }
